// Programa que pide la edad para conducir

let nombre = "José";
let edad = 27;
/* if (edad >= 18) {
    console.log(`${nombre} puede conducir`);
} else {
    console.log(`${nombre} puede conducir`);
}
 */

// Uso del operador and &&

/* if (edad < 18 && nombre === true) {
    console.log(`${nombre} es menor`);
} else {
    console.log(`${nombre} no es menor`);
}
 */

// Uso del operador or ||
let esta_pagando = false;
let esta_en_venta = true;

/* if (esta_pagando === true || !esta_en_venta === true) {
    console.log(`${nombre} puede ir a comprar`);
} else {
    console.log(`${nombre} no puede  comprar`);
}
 */


// Uso deelse if anidaddos
/* if (edad >= 18 && edad <= 30) {
    console.log("Es joven");
} else if (edad >= 31 && edad <= 40) {
    console.log("Es maduro");
} else if (edad >= 41 && edad <= 60) {
    console.log("Está en la tercera edad.")
} else {
    console.log("Edad no reconocida");
}

 */


// Uso del switch case
let fruta = "banana";

/* switch (fruta) {
    case "banana":
        console.log("Es una banana");
        break;
    case "manzana":
        console.log("Es una manzana");
        break;
    default:
        console.log("Fruta desconocida");
        break;
}

 */

